<% if flash[:notice] %>
     <div class="message-header notification is-info global-notification">
      <p class="notice"><%= notice %></p>
     </div>
    <% end %>
    <% if flash[:alert] %>
     <div class="message-header notification is-danger global-notification">
      <p class="alert"><%= alert %></p>
     </div>
    <% end %>
    <nav class="navbar is-primary">
      <div class="navbar-brand">
        <div class="navbar-item">
          <h1 class="title is-9">
            <%= link_to 'Railpedia', root_path, class: "has-text-white" %>
          </h1>
        </div>
      </div>
    </nav>